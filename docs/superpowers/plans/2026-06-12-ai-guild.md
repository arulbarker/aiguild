# AI Guild Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bangun platform pembelajaran vibe coding berbayar dengan modul flowchart bercabang, akses via magic link setelah beli di Lynk.id/Mayar.id, semua di VPS Hostinger via Coolify.

**Architecture:** Next.js 14 App Router (frontend + API) + PostgreSQL (Prisma ORM) + custom JWT magic-link auth. Semua container dimanage Coolify di VPS Ubuntu. Module content dikelola via kode/SQL langsung.

**Tech Stack:** Next.js 14, Prisma, PostgreSQL, jose (JWT), Resend (email), PostHog, Sentry, Vitest, Docker/Coolify

---

## File Structure

```
aiguild/
├── app/
│   ├── layout.js                        — root layout + providers
│   ├── page.js                          — landing page
│   ├── globals.css
│   ├── login/page.js                    — form request magic link
│   ├── auth/verify/route.js             — GET: verifikasi token, set cookie, redirect
│   ├── dashboard/page.js                — flowchart modul (server component)
│   ├── modul/[slug]/page.js             — module viewer full-screen
│   ├── admin/
│   │   ├── layout.js                    — admin layout + is_admin guard
│   │   ├── page.js                      — stats dashboard
│   │   ├── users/page.js                — daftar user + revoke
│   │   ├── modules/page.js              — daftar modul (read-only)
│   │   └── purchases/page.js            — riwayat pembelian
│   └── api/
│       ├── auth/
│       │   ├── send-link/route.js       — POST: kirim magic link
│       │   └── logout/route.js          — POST: hapus session cookie
│       ├── admin/
│       │   └── users/[id]/revoke/route.js — POST: revoke akses user
│       └── webhook/
│           ├── lynkid/route.js          — POST: webhook Lynk.id
│           └── mayar/route.js           — POST: webhook Mayar.id
├── components/
│   ├── ModuleFlowchart.js               — SVG DAG renderer (client)
│   ├── ModuleViewer.js                  — viewer tabs + sidebar (client)
│   └── Sidebar.js                       — collapsible sidebar (client)
├── lib/
│   ├── db.js                            — Prisma client singleton
│   ├── auth.js                          — createSession / getSession / clearSession
│   ├── email.js                         — sendMagicLink via Resend
│   ├── tokens.js                        — generateToken / hashToken
│   └── modules-seed.js                  — data modul (edit di sini)
├── middleware.js                         — proteksi /dashboard /modul /admin
├── prisma/schema.prisma
├── scripts/seed.js                      — jalankan untuk isi DB dengan modul
├── __tests__/
│   ├── tokens.test.js
│   └── webhooks.test.js
├── .env.example
├── Dockerfile
└── docker-compose.yml                   — postgres untuk local dev
```

---

## Phase 1: VPS + Project Foundation

### Task 1: Setup VPS — Install Coolify

- [ ] **Step 1: SSH ke VPS**

```bash
ssh root@<VPS_IP>
```

Expected: prompt shell Ubuntu.

- [ ] **Step 2: Update sistem**

```bash
apt update && apt upgrade -y
```

- [ ] **Step 3: Install Coolify (one-line official)**

```bash
curl -fsSL https://cdn.coollabs.io/coolify/install.sh | bash
```

Expected: script berjalan ~3-5 menit, di akhir muncul URL seperti:
```
Coolify is ready! Open http://<VPS_IP>:8000 in your browser.
```

- [ ] **Step 4: Buka browser, akses Coolify dashboard**

Buka `http://<VPS_IP>:8000` → daftar admin account → selesaikan onboarding.

- [ ] **Step 5: Tambah PostgreSQL di Coolify**

Di Coolify dashboard:
1. Klik **+ New Resource** → **Database** → **PostgreSQL**
2. Set nama: `aiguild-db`
3. Set password yang kuat → simpan di password manager
4. Klik **Deploy** — tunggu sampai status **Running**
5. Catat **Connection URL** yang diberikan Coolify (format: `postgresql://postgres:<password>@<host>:<port>/postgres`)

- [ ] **Step 6: Commit (kosong dulu — tandai milestone VPS siap)**

```bash
# Di local machine:
cd "D:/VIBE CODING VERSION/aiguild"
git init
git add .gitignore
git commit -m "chore: init repo dengan gitignore"
```

---

### Task 2: Init Next.js Project

- [ ] **Step 1: Buat project Next.js**

```bash
cd "D:/VIBE CODING VERSION/aiguild"
npx create-next-app@14 . --js --app --no-tailwind --no-src-dir --import-alias "@/*" --no-eslint
```

Jawab prompts: gunakan default semua.

- [ ] **Step 2: Install dependencies**

```bash
npm install prisma @prisma/client jose resend @sentry/nextjs
npm install -D vitest @vitest/coverage-v8
```

- [ ] **Step 3: Buat file .env.example**

```bash
# Tulis file ini
```

Isi `D:/VIBE CODING VERSION/aiguild/.env.example`:
```
DATABASE_URL=postgresql://postgres:password@localhost:5432/aiguild
JWT_SECRET=ganti-dengan-string-random-minimal-32-karakter
TOKEN_SECRET=ganti-dengan-string-random-minimal-32-karakter
RESEND_API_KEY=re_xxxxxxxxxxxx
NEXT_PUBLIC_APP_URL=https://aiguild.id
LYNKID_WEBHOOK_SECRET=secret-dari-lynkid
MAYAR_WEBHOOK_SECRET=secret-dari-mayar
NEXT_PUBLIC_POSTHOG_KEY=phc_xxxxxxxxxxxx
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com
SENTRY_DSN=https://xxxx@sentry.io/xxx
```

- [ ] **Step 4: Buat .env.local dari .env.example, isi nilai lokal**

```bash
cp .env.example .env.local
```

Edit `.env.local` — isi DATABASE_URL dengan postgres lokal (lihat Task 3).

- [ ] **Step 5: Tambah script test ke package.json**

Edit `package.json`, tambahkan di bagian `"scripts"`:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json .env.example next.config.mjs
git commit -m "chore: setup project Next.js 14 + dependencies"
```

---

### Task 3: Database Lokal + Prisma Schema

- [ ] **Step 1: Buat docker-compose.yml untuk postgres lokal**

Tulis `D:/VIBE CODING VERSION/aiguild/docker-compose.yml`:
```yaml
services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: aiguild
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: localpassword
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
```

- [ ] **Step 2: Jalankan postgres lokal**

```bash
docker compose up -d
```

Expected: `Container aiguild-postgres-1  Started`

- [ ] **Step 3: Update .env.local dengan koneksi lokal**

```
DATABASE_URL=postgresql://postgres:localpassword@localhost:5432/aiguild
JWT_SECRET=dev-jwt-secret-change-in-production-32chars
TOKEN_SECRET=dev-token-secret-change-in-production-32ch
```

- [ ] **Step 4: Init Prisma**

```bash
npx prisma init --datasource-provider postgresql
```

Expected: folder `prisma/` + `prisma/schema.prisma` terbuat.

- [ ] **Step 5: Tulis schema Prisma**

Tulis `D:/VIBE CODING VERSION/aiguild/prisma/schema.prisma`:
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id          String         @id @default(uuid())
  email       String         @unique
  name        String?
  isAdmin     Boolean        @default(false) @map("is_admin")
  createdAt   DateTime       @default(now()) @map("created_at")
  purchases   Purchase[]
  progress    UserProgress[]
  magicTokens MagicToken[]

  @@map("users")
}

model Module {
  id          String         @id @default(uuid())
  title       String
  slug        String         @unique
  description String?
  youtubeUrl  String?        @map("youtube_url")
  gammaUrl    String?        @map("gamma_url")
  parentIds   String[]       @default([]) @map("parent_ids")
  orderIndex  Int            @default(0) @map("order_index")
  createdAt   DateTime       @default(now()) @map("created_at")
  progress    UserProgress[]

  @@map("modules")
}

model Purchase {
  id          String   @id @default(uuid())
  user        User     @relation(fields: [userId], references: [id])
  userId      String   @map("user_id")
  source      String
  orderId     String?  @map("order_id")
  purchasedAt DateTime @default(now()) @map("purchased_at")

  @@map("purchases")
}

model UserProgress {
  user         User     @relation(fields: [userId], references: [id])
  userId       String   @map("user_id")
  module       Module   @relation(fields: [moduleId], references: [id])
  moduleId     String   @map("module_id")
  lastViewedAt DateTime @default(now()) @updatedAt @map("last_viewed_at")

  @@id([userId, moduleId])
  @@map("user_progress")
}

model MagicToken {
  id        String   @id @default(uuid())
  email     String
  token     String   @unique
  expiresAt DateTime @map("expires_at")
  used      Boolean  @default(false)
  createdAt DateTime @default(now()) @map("created_at")
  user      User?    @relation(fields: [email], references: [email])

  @@map("magic_tokens")
}
```

- [ ] **Step 6: Jalankan migrasi**

```bash
npx prisma migrate dev --name init
```

Expected:
```
✔ Generated Prisma Client
The following migration(s) have been applied: 20260612000000_init
```

- [ ] **Step 7: Buat lib/db.js — Prisma singleton**

Tulis `D:/VIBE CODING VERSION/aiguild/lib/db.js`:
```js
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis

export const prisma = globalForPrisma.prisma ?? new PrismaClient()

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma
}
```

- [ ] **Step 8: Commit**

```bash
git add prisma/ lib/db.js docker-compose.yml
git commit -m "feat: prisma schema + postgres local dev setup"
```

---

## Phase 2: Auth System

### Task 4: Token Library + Tests

- [ ] **Step 1: Tulis test dulu (TDD)**

Tulis `D:/VIBE CODING VERSION/aiguild/__tests__/tokens.test.js`:
```js
import { describe, it, expect, beforeAll } from 'vitest'
import { generateToken, hashToken } from '../lib/tokens.js'

beforeAll(() => {
  process.env.TOKEN_SECRET = 'test-secret-32-chars-minimum-here'
})

describe('generateToken', () => {
  it('returns 64-character hex string', () => {
    const token = generateToken()
    expect(token).toHaveLength(64)
    expect(token).toMatch(/^[a-f0-9]+$/)
  })

  it('generates unique tokens on each call', () => {
    const t1 = generateToken()
    const t2 = generateToken()
    expect(t1).not.toBe(t2)
  })
})

describe('hashToken', () => {
  it('returns same hash for same input', () => {
    const h1 = hashToken('abc123')
    const h2 = hashToken('abc123')
    expect(h1).toBe(h2)
  })

  it('returns different hash for different input', () => {
    expect(hashToken('abc')).not.toBe(hashToken('xyz'))
  })

  it('returns 64-character hex hash', () => {
    expect(hashToken('test')).toHaveLength(64)
    expect(hashToken('test')).toMatch(/^[a-f0-9]+$/)
  })
})
```

- [ ] **Step 2: Jalankan test — pastikan FAIL**

```bash
npm test -- --reporter=verbose __tests__/tokens.test.js
```

Expected: `FAIL — Cannot find module '../lib/tokens.js'`

- [ ] **Step 3: Implementasi lib/tokens.js**

Tulis `D:/VIBE CODING VERSION/aiguild/lib/tokens.js`:
```js
import { randomBytes, createHmac } from 'crypto'

export function generateToken() {
  return randomBytes(32).toString('hex')
}

export function hashToken(token) {
  return createHmac('sha256', process.env.TOKEN_SECRET)
    .update(token)
    .digest('hex')
}
```

- [ ] **Step 4: Jalankan test — pastikan PASS**

```bash
npm test -- --reporter=verbose __tests__/tokens.test.js
```

Expected: `✓ tokens.test.js (5 tests) — all passed`

- [ ] **Step 5: Commit**

```bash
git add lib/tokens.js __tests__/tokens.test.js
git commit -m "feat: token generation dan hashing library"
```

---

### Task 5: Auth Library (JWT Cookie)

- [ ] **Step 1: Tulis lib/auth.js**

Tulis `D:/VIBE CODING VERSION/aiguild/lib/auth.js`:
```js
import { SignJWT, jwtVerify } from 'jose'
import { cookies } from 'next/headers'

const COOKIE_NAME = 'aiguild_session'
const MAX_AGE_SECONDS = 60 * 60 * 24 * 30 // 30 hari

function getSecret() {
  return new TextEncoder().encode(process.env.JWT_SECRET)
}

export async function createSession(userId, isAdmin) {
  const token = await new SignJWT({ userId, isAdmin })
    .setProtectedHeader({ alg: 'HS256' })
    .setIssuedAt()
    .setExpirationTime('30d')
    .sign(getSecret())

  const cookieStore = await cookies()
  cookieStore.set(COOKIE_NAME, token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: MAX_AGE_SECONDS,
    path: '/',
  })
}

export async function getSession() {
  const cookieStore = await cookies()
  const token = cookieStore.get(COOKIE_NAME)?.value
  if (!token) return null

  try {
    const { payload } = await jwtVerify(token, getSecret())
    return payload
  } catch {
    return null
  }
}

export async function clearSession() {
  const cookieStore = await cookies()
  cookieStore.delete(COOKIE_NAME)
}
```

- [ ] **Step 2: Commit**

```bash
git add lib/auth.js
git commit -m "feat: JWT session helpers (create/get/clear)"
```

---

### Task 6: Email Library (Resend)

- [ ] **Step 1: Tulis lib/email.js**

Tulis `D:/VIBE CODING VERSION/aiguild/lib/email.js`:
```js
import { Resend } from 'resend'

const resend = new Resend(process.env.RESEND_API_KEY)

export async function sendMagicLink(email, token) {
  const url = `${process.env.NEXT_PUBLIC_APP_URL}/auth/verify?token=${token}`

  await resend.emails.send({
    from: 'AI Guild <noreply@aiguild.id>',
    to: email,
    subject: 'Link masuk ke AI Guild',
    html: `
      <div style="font-family:sans-serif;max-width:480px;margin:0 auto;padding:32px;">
        <h2 style="color:#7c3aed;">AI Guild</h2>
        <p>Klik tombol di bawah untuk masuk. Link berlaku <strong>15 menit</strong>.</p>
        <a href="${url}"
           style="display:inline-block;background:#7c3aed;color:white;
                  padding:12px 28px;border-radius:8px;text-decoration:none;
                  font-weight:600;margin:16px 0;">
          Masuk ke AI Guild
        </a>
        <p style="color:#6b7280;font-size:0.85rem;">
          Jika tombol tidak bisa diklik, copy link ini:<br/>
          <a href="${url}" style="color:#7c3aed;">${url}</a>
        </p>
        <p style="color:#6b7280;font-size:0.8rem;">
          Jika kamu tidak meminta link ini, abaikan email ini.
        </p>
      </div>
    `,
  })
}
```

- [ ] **Step 2: Daftar akun Resend**

Buka `https://resend.com` → daftar gratis → buat API key → simpan di `.env.local` sebagai `RESEND_API_KEY`.

Untuk domain email: verifikasi domain kamu di Resend agar bisa pakai `noreply@aiguild.id`. Sementara dev pakai `onboarding@resend.dev`.

- [ ] **Step 3: Commit**

```bash
git add lib/email.js
git commit -m "feat: email magic link sender via Resend"
```

---

### Task 7: API Route — Send Magic Link

- [ ] **Step 1: Buat folder dan route**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/auth/send-link/route.js`:
```js
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db'
import { generateToken, hashToken } from '@/lib/tokens'
import { sendMagicLink } from '@/lib/email'

export async function POST(request) {
  let body
  try {
    body = await request.json()
  } catch {
    return NextResponse.json({ error: 'Request tidak valid' }, { status: 400 })
  }

  const email = body?.email?.toLowerCase().trim()
  if (!email || !email.includes('@') || !email.includes('.')) {
    return NextResponse.json({ error: 'Email tidak valid' }, { status: 400 })
  }

  const user = await prisma.user.findUnique({ where: { email } })
  if (!user) {
    // Sengaja return OK agar tidak bocorkan email mana yang terdaftar
    return NextResponse.json({ ok: true })
  }

  const rawToken = generateToken()
  const hashedToken = hashToken(rawToken)
  const expiresAt = new Date(Date.now() + 15 * 60 * 1000)

  await prisma.magicToken.create({
    data: { email, token: hashedToken, expiresAt },
  })

  await sendMagicLink(email, rawToken)
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 2: Commit**

```bash
git add app/api/auth/send-link/
git commit -m "feat: API route send magic link"
```

---

### Task 8: Route Handler — Verify Token & Create Session

- [ ] **Step 1: Buat route handler verify**

Tulis `D:/VIBE CODING VERSION/aiguild/app/auth/verify/route.js`:
```js
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db'
import { hashToken } from '@/lib/tokens'
import { createSession } from '@/lib/auth'

export async function GET(request) {
  const { searchParams, origin } = new URL(request.url)
  const rawToken = searchParams.get('token')

  if (!rawToken) {
    return NextResponse.redirect(`${origin}/login?error=invalid`)
  }

  const hashedToken = hashToken(rawToken)

  const magicToken = await prisma.magicToken.findFirst({
    where: {
      token: hashedToken,
      used: false,
      expiresAt: { gt: new Date() },
    },
  })

  if (!magicToken) {
    return NextResponse.redirect(`${origin}/login?error=expired`)
  }

  const user = await prisma.user.findUnique({
    where: { email: magicToken.email },
  })

  if (!user) {
    return NextResponse.redirect(`${origin}/login?error=invalid`)
  }

  await prisma.magicToken.update({
    where: { id: magicToken.id },
    data: { used: true },
  })

  await createSession(user.id, user.isAdmin)
  return NextResponse.redirect(`${origin}/dashboard`)
}
```

- [ ] **Step 2: Buat route logout**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/auth/logout/route.js`:
```js
import { NextResponse } from 'next/server'
import { clearSession } from '@/lib/auth'

export async function POST() {
  await clearSession()
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 3: Commit**

```bash
git add app/auth/verify/ app/api/auth/logout/
git commit -m "feat: verify magic link + logout route"
```

---

### Task 9: Middleware — Proteksi Routes

- [ ] **Step 1: Tulis middleware.js**

Tulis `D:/VIBE CODING VERSION/aiguild/middleware.js`:
```js
import { NextResponse } from 'next/server'
import { jwtVerify } from 'jose'

const COOKIE_NAME = 'aiguild_session'

function getSecret() {
  return new TextEncoder().encode(process.env.JWT_SECRET)
}

export async function middleware(request) {
  const { pathname } = request.nextUrl
  const token = request.cookies.get(COOKIE_NAME)?.value

  if (!token) {
    return NextResponse.redirect(new URL('/login', request.url))
  }

  try {
    const { payload } = await jwtVerify(token, getSecret())

    if (pathname.startsWith('/admin') && !payload.isAdmin) {
      return NextResponse.redirect(new URL('/dashboard', request.url))
    }

    return NextResponse.next()
  } catch {
    const response = NextResponse.redirect(new URL('/login', request.url))
    response.cookies.delete(COOKIE_NAME)
    return response
  }
}

export const config = {
  matcher: ['/dashboard/:path*', '/modul/:path*', '/admin/:path*'],
}
```

- [ ] **Step 2: Commit**

```bash
git add middleware.js
git commit -m "feat: middleware proteksi dashboard + modul + admin routes"
```

---

### Task 10: Halaman Login

- [ ] **Step 1: Tulis app/login/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/login/page.js`:
```js
'use client'

import { useState } from 'react'
import { useSearchParams } from 'next/navigation'

export default function LoginPage() {
  const [email, setEmail] = useState('')
  const [status, setStatus] = useState('idle') // idle | loading | sent | error
  const searchParams = useSearchParams()
  const errorParam = searchParams.get('error')

  async function handleSubmit(e) {
    e.preventDefault()
    setStatus('loading')

    const res = await fetch('/api/auth/send-link', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email }),
    })

    setStatus(res.ok ? 'sent' : 'error')
  }

  return (
    <div style={{
      minHeight: '100vh',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      background: '#0a0a16',
      padding: '1rem',
    }}>
      <div style={{
        background: '#12122a',
        border: '1px solid #1e1e3a',
        borderRadius: '16px',
        padding: '2.5rem',
        width: '100%',
        maxWidth: '400px',
      }}>
        <h1 style={{ color: '#a78bfa', marginBottom: '0.5rem', fontSize: '1.5rem' }}>
          AI Guild
        </h1>
        <p style={{ color: '#6b7280', marginBottom: '2rem', fontSize: '0.9rem' }}>
          Masukkan email yang kamu gunakan saat beli kelas.
        </p>

        {errorParam === 'expired' && (
          <p style={{ color: '#f87171', marginBottom: '1rem', fontSize: '0.85rem' }}>
            Link sudah kadaluarsa. Minta link baru di bawah.
          </p>
        )}
        {errorParam === 'invalid' && (
          <p style={{ color: '#f87171', marginBottom: '1rem', fontSize: '0.85rem' }}>
            Link tidak valid. Coba lagi.
          </p>
        )}

        {status === 'sent' ? (
          <div style={{ textAlign: 'center', color: '#4ade80' }}>
            <p style={{ fontSize: '1.1rem', marginBottom: '0.5rem' }}>✓ Link terkirim!</p>
            <p style={{ color: '#9ca3af', fontSize: '0.85rem' }}>
              Cek inbox email kamu. Link berlaku 15 menit.
            </p>
          </div>
        ) : (
          <form onSubmit={handleSubmit}>
            <input
              type="email"
              value={email}
              onChange={e => setEmail(e.target.value)}
              placeholder="email@kamu.com"
              required
              style={{
                width: '100%',
                background: '#0a0a16',
                border: '1px solid #2d2d4a',
                borderRadius: '8px',
                padding: '12px 16px',
                color: '#e2e8f0',
                fontSize: '1rem',
                marginBottom: '1rem',
                boxSizing: 'border-box',
                outline: 'none',
              }}
            />
            <button
              type="submit"
              disabled={status === 'loading'}
              style={{
                width: '100%',
                background: status === 'loading' ? '#4b3a8a' : '#7c3aed',
                color: 'white',
                border: 'none',
                borderRadius: '8px',
                padding: '12px',
                fontSize: '1rem',
                fontWeight: '600',
                cursor: status === 'loading' ? 'not-allowed' : 'pointer',
              }}
            >
              {status === 'loading' ? 'Mengirim...' : 'Kirim Link Masuk'}
            </button>
            {status === 'error' && (
              <p style={{ color: '#f87171', fontSize: '0.85rem', marginTop: '0.75rem', textAlign: 'center' }}>
                Gagal kirim. Coba lagi.
              </p>
            )}
          </form>
        )}
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Test manual**

```bash
npm run dev
```

Buka `http://localhost:3000/login` — pastikan form muncul, submit dengan email test, cek tidak ada error di terminal.

- [ ] **Step 3: Commit**

```bash
git add app/login/
git commit -m "feat: halaman login dengan magic link form"
```

---

## Phase 3: Webhook Handlers

### Task 11: Webhook Lynk.id + Tests

- [ ] **Step 1: Tulis test webhook**

Tulis `D:/VIBE CODING VERSION/aiguild/__tests__/webhooks.test.js`:
```js
import { describe, it, expect, vi, beforeAll } from 'vitest'
import { createHmac } from 'crypto'

// Helper buat signature Lynk.id
function makeLynkidSignature(body, secret) {
  return createHmac('sha256', secret).update(JSON.stringify(body)).digest('hex')
}

// Helper buat signature Mayar
function makeMayarSignature(body, secret) {
  return createHmac('sha256', secret).update(JSON.stringify(body)).digest('hex')
}

describe('Lynk.id webhook signature', () => {
  const secret = 'test-lynkid-secret'
  const payload = { event: 'order.paid', data: { buyer_email: 'test@example.com', order_id: 'LNK-001' } }

  it('valid signature diterima', () => {
    const sig = makeLynkidSignature(payload, secret)
    const computed = createHmac('sha256', secret).update(JSON.stringify(payload)).digest('hex')
    expect(sig).toBe(computed)
  })

  it('signature yang salah berbeda', () => {
    const validSig = makeLynkidSignature(payload, secret)
    const wrongSig = makeLynkidSignature(payload, 'wrong-secret')
    expect(validSig).not.toBe(wrongSig)
  })
})

describe('Mayar webhook signature', () => {
  const secret = 'test-mayar-secret'
  const payload = { event: 'payment.paid', data: { email: 'test@example.com', invoice_id: 'MAY-001' } }

  it('valid signature diterima', () => {
    const sig = makeMayarSignature(payload, secret)
    const computed = createHmac('sha256', secret).update(JSON.stringify(payload)).digest('hex')
    expect(sig).toBe(computed)
  })
})
```

- [ ] **Step 2: Jalankan test**

```bash
npm test -- --reporter=verbose __tests__/webhooks.test.js
```

Expected: `✓ all passed`

- [ ] **Step 3: Tulis webhook Lynk.id**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/webhook/lynkid/route.js`:
```js
import { NextResponse } from 'next/server'
import { createHmac, timingSafeEqual } from 'crypto'
import { prisma } from '@/lib/db'
import { sendMagicLink } from '@/lib/email'
import { generateToken, hashToken } from '@/lib/tokens'

function verifySignature(body, signature) {
  const expected = createHmac('sha256', process.env.LYNKID_WEBHOOK_SECRET)
    .update(body)
    .digest('hex')
  try {
    return timingSafeEqual(Buffer.from(signature), Buffer.from(expected))
  } catch {
    return false
  }
}

export async function POST(request) {
  const rawBody = await request.text()
  const signature = request.headers.get('x-lynkid-signature') ?? ''

  if (!verifySignature(rawBody, signature)) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  let payload
  try {
    payload = JSON.parse(rawBody)
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  // Lynk.id payload: { event: "order.paid", data: { buyer_email, order_id } }
  if (payload.event !== 'order.paid') {
    return NextResponse.json({ ok: true }) // event lain diabaikan
  }

  const email = payload.data?.buyer_email?.toLowerCase().trim()
  const orderId = payload.data?.order_id

  if (!email) {
    return NextResponse.json({ error: 'Email tidak ada di payload' }, { status: 400 })
  }

  // Upsert user
  const user = await prisma.user.upsert({
    where: { email },
    update: {},
    create: { email },
  })

  // Simpan purchase (cek duplikat by orderId)
  const existing = await prisma.purchase.findFirst({ where: { orderId } })
  if (!existing) {
    await prisma.purchase.create({
      data: { userId: user.id, source: 'lynkid', orderId },
    })
  }

  // Kirim magic link
  const rawToken = generateToken()
  const hashedToken = hashToken(rawToken)
  const expiresAt = new Date(Date.now() + 24 * 60 * 60 * 1000) // 24 jam untuk link pertama kali

  await prisma.magicToken.create({
    data: { email, token: hashedToken, expiresAt },
  })

  await sendMagicLink(email, rawToken)
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 4: Commit**

```bash
git add app/api/webhook/lynkid/ __tests__/webhooks.test.js
git commit -m "feat: webhook Lynk.id dengan verifikasi signature"
```

---

### Task 12: Webhook Mayar.id

- [ ] **Step 1: Tulis webhook Mayar.id**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/webhook/mayar/route.js`:
```js
import { NextResponse } from 'next/server'
import { createHmac, timingSafeEqual } from 'crypto'
import { prisma } from '@/lib/db'
import { sendMagicLink } from '@/lib/email'
import { generateToken, hashToken } from '@/lib/tokens'

function verifySignature(body, signature) {
  const expected = createHmac('sha256', process.env.MAYAR_WEBHOOK_SECRET)
    .update(body)
    .digest('hex')
  try {
    return timingSafeEqual(Buffer.from(signature), Buffer.from(expected))
  } catch {
    return false
  }
}

export async function POST(request) {
  const rawBody = await request.text()
  const signature = request.headers.get('x-mayar-signature') ?? ''

  if (!verifySignature(rawBody, signature)) {
    return NextResponse.json({ error: 'Unauthorized' }, { status: 401 })
  }

  let payload
  try {
    payload = JSON.parse(rawBody)
  } catch {
    return NextResponse.json({ error: 'Invalid JSON' }, { status: 400 })
  }

  // Mayar payload: { event: "payment.paid", data: { email, invoice_id } }
  if (payload.event !== 'payment.paid') {
    return NextResponse.json({ ok: true })
  }

  const email = payload.data?.email?.toLowerCase().trim()
  const orderId = payload.data?.invoice_id

  if (!email) {
    return NextResponse.json({ error: 'Email tidak ada di payload' }, { status: 400 })
  }

  const user = await prisma.user.upsert({
    where: { email },
    update: {},
    create: { email },
  })

  const existing = await prisma.purchase.findFirst({ where: { orderId } })
  if (!existing) {
    await prisma.purchase.create({
      data: { userId: user.id, source: 'mayar', orderId },
    })
  }

  const rawToken = generateToken()
  const hashedToken = hashToken(rawToken)
  const expiresAt = new Date(Date.now() + 24 * 60 * 60 * 1000)

  await prisma.magicToken.create({
    data: { email, token: hashedToken, expiresAt },
  })

  await sendMagicLink(email, rawToken)
  return NextResponse.json({ ok: true })
}
```

**Catatan penting:** Format payload dan nama header signature Lynk.id & Mayar.id perlu dikonfirmasi dengan dokumentasi resmi masing-masing platform sebelum live. Sesuaikan `payload.event`, `payload.data.buyer_email`, dan nama header jika berbeda.

- [ ] **Step 2: Commit**

```bash
git add app/api/webhook/mayar/
git commit -m "feat: webhook Mayar.id dengan verifikasi signature"
```

---

## Phase 4: Core UI

### Task 13: Data Modul (Seed)

- [ ] **Step 1: Tulis lib/modules-seed.js**

Tulis `D:/VIBE CODING VERSION/aiguild/lib/modules-seed.js`:
```js
// Edit file ini untuk menambah/ubah modul kelas
// Setelah edit, jalankan: node scripts/seed.js
export const MODULES_DATA = [
  {
    slug: 'intro-vibe-coding',
    title: 'Intro Vibe Coding',
    description: 'Apa itu vibe coding, kenapa ini masa depan, dan bagaimana kita akan belajar.',
    youtubeUrl: 'https://www.youtube.com/embed/GANTI_ID_YOUTUBE',
    gammaUrl: 'https://gamma.app/embed/GANTI_ID_GAMMA',
    parentIds: [],
    orderIndex: 0,
  },
  {
    slug: 'setup-environment',
    title: 'Setup Tools & Environment',
    description: 'Install semua tools yang dibutuhkan untuk mulai vibe coding.',
    youtubeUrl: 'https://www.youtube.com/embed/GANTI_ID_YOUTUBE',
    gammaUrl: 'https://gamma.app/embed/GANTI_ID_GAMMA',
    parentIds: ['intro-vibe-coding'],
    orderIndex: 1,
  },
  {
    slug: 'build-project-pertama',
    title: 'Build Project Pertama',
    description: 'Bangun project nyata dari nol menggunakan Claude Code.',
    youtubeUrl: 'https://www.youtube.com/embed/GANTI_ID_YOUTUBE',
    gammaUrl: 'https://gamma.app/embed/GANTI_ID_GAMMA',
    parentIds: ['setup-environment'],
    orderIndex: 2,
  },
  {
    slug: 'deploy-ke-vps',
    title: 'Deploy ke VPS dengan Coolify',
    description: 'Cara deploy aplikasi ke VPS menggunakan Coolify step by step.',
    youtubeUrl: 'https://www.youtube.com/embed/GANTI_ID_YOUTUBE',
    gammaUrl: 'https://gamma.app/embed/GANTI_ID_GAMMA',
    parentIds: ['build-project-pertama'],
    orderIndex: 3,
  },
]
```

- [ ] **Step 2: Tulis scripts/seed.js**

Tulis `D:/VIBE CODING VERSION/aiguild/scripts/seed.js`:
```js
import { PrismaClient } from '@prisma/client'
import { MODULES_DATA } from '../lib/modules-seed.js'

const prisma = new PrismaClient()

async function main() {
  console.log('Seeding modules...')

  for (const mod of MODULES_DATA) {
    await prisma.module.upsert({
      where: { slug: mod.slug },
      update: {
        title: mod.title,
        description: mod.description,
        youtubeUrl: mod.youtubeUrl,
        gammaUrl: mod.gammaUrl,
        parentIds: mod.parentIds,
        orderIndex: mod.orderIndex,
      },
      create: {
        title: mod.title,
        slug: mod.slug,
        description: mod.description,
        youtubeUrl: mod.youtubeUrl,
        gammaUrl: mod.gammaUrl,
        parentIds: mod.parentIds,
        orderIndex: mod.orderIndex,
      },
    })
    console.log(`✓ ${mod.slug}`)
  }

  console.log('Done!')
  await prisma.$disconnect()
}

main().catch(e => { console.error(e); process.exit(1) })
```

- [ ] **Step 3: Tambah script ke package.json**

Edit `package.json`, tambah di `"scripts"`:
```json
"seed": "node --experimental-vm-modules scripts/seed.js"
```

- [ ] **Step 4: Jalankan seed**

```bash
npm run seed
```

Expected:
```
Seeding modules...
✓ intro-vibe-coding
✓ setup-environment
✓ build-project-pertama
✓ deploy-ke-vps
Done!
```

- [ ] **Step 5: Commit**

```bash
git add lib/modules-seed.js scripts/seed.js package.json
git commit -m "feat: module seed data + seed script"
```

---

### Task 14: Dashboard — Flowchart Modul

- [ ] **Step 1: Tulis komponen ModuleFlowchart**

Tulis `D:/VIBE CODING VERSION/aiguild/components/ModuleFlowchart.js`:
```js
'use client'

import { useRouter } from 'next/navigation'

function buildTree(modules) {
  const bySlug = {}
  modules.forEach(m => { bySlug[m.slug] = { ...m, children: [] } })

  const roots = []
  modules.forEach(m => {
    if (!m.parentIds || m.parentIds.length === 0) {
      roots.push(bySlug[m.slug])
    } else {
      m.parentIds.forEach(parentSlug => {
        if (bySlug[parentSlug]) {
          bySlug[parentSlug].children.push(bySlug[m.slug])
        }
      })
    }
  })
  return roots
}

function ModuleNode({ module: mod, depth = 0 }) {
  const router = useRouter()

  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <div
        onClick={() => router.push(`/modul/${mod.slug}`)}
        style={{
          background: '#1e1e3a',
          border: '2px solid #7c3aed',
          borderRadius: '12px',
          padding: '14px 28px',
          minWidth: '200px',
          maxWidth: '360px',
          textAlign: 'center',
          cursor: 'pointer',
          boxShadow: '0 0 20px #7c3aed22',
          transition: 'transform 0.15s, box-shadow 0.15s',
          color: '#e2e8f0',
          fontWeight: '600',
        }}
        onMouseEnter={e => {
          e.currentTarget.style.transform = 'translateY(-2px)'
          e.currentTarget.style.boxShadow = '0 4px 24px #7c3aed44'
        }}
        onMouseLeave={e => {
          e.currentTarget.style.transform = 'translateY(0)'
          e.currentTarget.style.boxShadow = '0 0 20px #7c3aed22'
        }}
      >
        <div style={{ fontSize: '0.7rem', color: '#7c3aed', letterSpacing: '0.1em', marginBottom: '4px' }}>
          MODUL
        </div>
        <div>{mod.title}</div>
      </div>

      {mod.children.length > 0 && (
        <>
          {mod.children.length === 1 ? (
            <ConnectorLine />
          ) : (
            <BranchConnector count={mod.children.length} />
          )}

          <div style={{
            display: 'flex',
            flexDirection: mod.children.length > 1 ? 'row' : 'column',
            gap: mod.children.length > 1 ? '3rem' : '0',
            alignItems: 'flex-start',
          }}>
            {mod.children.map(child => (
              <ModuleNode key={child.slug} module={child} depth={depth + 1} />
            ))}
          </div>
        </>
      )}
    </div>
  )
}

function ConnectorLine() {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '2px' }}>
      <div style={{ width: '2px', height: '24px', background: 'linear-gradient(#7c3aed, #6d28d9)' }} />
      <div style={{
        width: 0, height: 0,
        borderLeft: '6px solid transparent',
        borderRight: '6px solid transparent',
        borderTop: '8px solid #6d28d9',
      }} />
    </div>
  )
}

function BranchConnector({ count }) {
  return (
    <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}>
      <div style={{ width: '2px', height: '16px', background: '#7c3aed' }} />
      <div style={{
        width: `${(count - 1) * 180 + 4}px`,
        height: '2px',
        background: '#7c3aed',
        position: 'relative',
      }} />
      <div style={{ display: 'flex', gap: `${(count - 1) * 180 - 2}px` }}>
        {Array.from({ length: count }).map((_, i) => (
          <div key={i} style={{ width: '2px', height: '16px', background: '#7c3aed' }} />
        ))}
      </div>
    </div>
  )
}

export default function ModuleFlowchart({ modules }) {
  const roots = buildTree(modules)

  return (
    <div style={{
      display: 'flex',
      flexDirection: 'column',
      alignItems: 'center',
      gap: '0',
      padding: '2rem 1rem',
      overflowX: 'auto',
    }}>
      {roots.map(root => (
        <ModuleNode key={root.slug} module={root} />
      ))}
    </div>
  )
}
```

- [ ] **Step 2: Tulis halaman Dashboard**

Tulis `D:/VIBE CODING VERSION/aiguild/app/dashboard/page.js`:
```js
import { prisma } from '@/lib/db'
import { getSession } from '@/lib/auth'
import ModuleFlowchart from '@/components/ModuleFlowchart'

export default async function DashboardPage() {
  const session = await getSession()
  const modules = await prisma.module.findMany({
    orderBy: { orderIndex: 'asc' },
  })

  return (
    <div style={{ minHeight: '100vh', background: '#0a0a16' }}>
      <header style={{
        background: '#12122a',
        borderBottom: '1px solid #1e1e3a',
        padding: '1rem 1.5rem',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
      }}>
        <h1 style={{ color: '#a78bfa', fontSize: '1.2rem', margin: 0 }}>AI Guild</h1>
        <form action="/api/auth/logout" method="POST">
          <button
            type="submit"
            style={{
              background: 'transparent',
              border: '1px solid #374151',
              color: '#9ca3af',
              padding: '6px 14px',
              borderRadius: '6px',
              cursor: 'pointer',
              fontSize: '0.85rem',
            }}
          >
            Keluar
          </button>
        </form>
      </header>

      <main style={{ padding: '2rem 1rem' }}>
        <div style={{ textAlign: 'center', marginBottom: '2.5rem' }}>
          <h2 style={{ color: '#e2e8f0', marginBottom: '0.5rem' }}>Roadmap Pembelajaran</h2>
          <p style={{ color: '#6b7280', fontSize: '0.9rem' }}>
            Klik modul untuk mulai belajar
          </p>
        </div>
        <ModuleFlowchart modules={modules} />
      </main>
    </div>
  )
}
```

- [ ] **Step 3: Test manual**

```bash
npm run dev
```

1. Buka `http://localhost:3000/dashboard` — harus redirect ke `/login` (middleware bekerja)
2. Buat user test langsung di DB: `npx prisma studio` → buka tabel `users` → tambah row dengan email kamu
3. Jalankan send-link via curl:
   ```bash
   curl -X POST http://localhost:3000/api/auth/send-link \
     -H "Content-Type: application/json" \
     -d '{"email":"email-test-kamu@example.com"}'
   ```
4. Cek terminal — URL magic link muncul di log Resend (mode dev)
5. Klik link → harus redirect ke `/dashboard` dan tampil flowchart

- [ ] **Step 4: Commit**

```bash
git add components/ModuleFlowchart.js app/dashboard/
git commit -m "feat: dashboard flowchart modul bercabang"
```

---

### Task 15: Module Viewer Full-Screen

- [ ] **Step 1: Tulis komponen Sidebar**

Tulis `D:/VIBE CODING VERSION/aiguild/components/Sidebar.js`:
```js
'use client'

import { useState } from 'react'
import Link from 'next/link'

export default function Sidebar({ modules, currentSlug }) {
  const [open, setOpen] = useState(false)

  return (
    <>
      {/* Strip collapsed */}
      <div style={{
        width: open ? '260px' : '28px',
        background: '#12122a',
        borderRight: '1px solid #1e1e3a',
        display: 'flex',
        flexDirection: 'column',
        transition: 'width 0.2s ease',
        overflow: 'hidden',
        flexShrink: 0,
        height: '100vh',
        position: 'sticky',
        top: 0,
      }}>
        <button
          onClick={() => setOpen(o => !o)}
          style={{
            background: 'transparent',
            border: 'none',
            color: '#7c3aed',
            padding: '16px 0',
            cursor: 'pointer',
            fontSize: '1.1rem',
            flexShrink: 0,
            width: '28px',
          }}
        >
          {open ? '✕' : '☰'}
        </button>

        {!open && (
          <div style={{ display: 'flex', flexDirection: 'column', alignItems: 'center', gap: '8px', paddingTop: '8px' }}>
            {modules.map(m => (
              <div
                key={m.slug}
                style={{
                  width: '8px', height: '8px',
                  borderRadius: '50%',
                  background: m.slug === currentSlug ? '#7c3aed' : '#2d2d4a',
                  boxShadow: m.slug === currentSlug ? '0 0 8px #7c3aed' : 'none',
                }}
              />
            ))}
          </div>
        )}

        {open && (
          <nav style={{ padding: '0.5rem', overflowY: 'auto', flex: 1 }}>
            <p style={{ color: '#6b7280', fontSize: '0.7rem', padding: '8px 8px 4px', letterSpacing: '0.1em' }}>
              MODUL
            </p>
            {modules.map(m => (
              <Link
                key={m.slug}
                href={`/modul/${m.slug}`}
                style={{
                  display: 'block',
                  padding: '10px 12px',
                  borderRadius: '6px',
                  color: m.slug === currentSlug ? '#a78bfa' : '#9ca3af',
                  background: m.slug === currentSlug ? '#1e1e3a' : 'transparent',
                  textDecoration: 'none',
                  fontSize: '0.85rem',
                  borderLeft: m.slug === currentSlug ? '3px solid #7c3aed' : '3px solid transparent',
                  marginBottom: '2px',
                }}
              >
                {m.title}
              </Link>
            ))}
          </nav>
        )}
      </div>
    </>
  )
}
```

- [ ] **Step 2: Tulis halaman Module Viewer**

Tulis `D:/VIBE CODING VERSION/aiguild/app/modul/[slug]/page.js`:
```js
import { notFound } from 'next/navigation'
import { prisma } from '@/lib/db'
import { getSession } from '@/lib/auth'
import ModuleViewer from '@/components/ModuleViewer'

export default async function ModulPage({ params }) {
  const { slug } = await params
  const session = await getSession()

  const [module, allModules] = await Promise.all([
    prisma.module.findUnique({ where: { slug } }),
    prisma.module.findMany({ orderBy: { orderIndex: 'asc' } }),
  ])

  if (!module) notFound()

  // Update progress
  await prisma.userProgress.upsert({
    where: { userId_moduleId: { userId: session.userId, moduleId: module.id } },
    update: { lastViewedAt: new Date() },
    create: { userId: session.userId, moduleId: module.id },
  })

  const currentIndex = allModules.findIndex(m => m.slug === slug)
  const prevModule = currentIndex > 0 ? allModules[currentIndex - 1] : null
  const nextModule = currentIndex < allModules.length - 1 ? allModules[currentIndex + 1] : null

  return (
    <ModuleViewer
      module={module}
      allModules={allModules}
      prevModule={prevModule}
      nextModule={nextModule}
      currentIndex={currentIndex}
      total={allModules.length}
    />
  )
}
```

- [ ] **Step 3: Tulis komponen ModuleViewer**

Tulis `D:/VIBE CODING VERSION/aiguild/components/ModuleViewer.js`:
```js
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import Sidebar from './Sidebar'

export default function ModuleViewer({ module: mod, allModules, prevModule, nextModule, currentIndex, total }) {
  const [activeTab, setActiveTab] = useState('video')
  const router = useRouter()

  return (
    <div style={{ display: 'flex', height: '100vh', background: '#0a0a16', overflow: 'hidden' }}>
      <Sidebar modules={allModules} currentSlug={mod.slug} />

      <div style={{ flex: 1, display: 'flex', flexDirection: 'column', overflow: 'hidden' }}>
        {/* Header */}
        <div style={{
          background: '#12122a',
          borderBottom: '1px solid #1e1e3a',
          padding: '12px 20px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          flexShrink: 0,
        }}>
          <div>
            <span style={{ color: '#7c3aed', fontSize: '0.7rem', letterSpacing: '0.1em' }}>
              MODUL {currentIndex + 1} / {total}
            </span>
            <h2 style={{ color: '#e2e8f0', margin: '2px 0 0', fontSize: '1rem', fontWeight: '600' }}>
              {mod.title}
            </h2>
          </div>

          <div style={{ display: 'flex', gap: '8px' }}>
            {['video', 'materi'].map(tab => (
              <button
                key={tab}
                onClick={() => setActiveTab(tab)}
                style={{
                  background: activeTab === tab ? '#7c3aed' : '#1e1e3a',
                  border: 'none',
                  color: activeTab === tab ? 'white' : '#9ca3af',
                  padding: '6px 16px',
                  borderRadius: '20px',
                  cursor: 'pointer',
                  fontSize: '0.85rem',
                  fontWeight: activeTab === tab ? '600' : '400',
                }}
              >
                {tab === 'video' ? '▶ Video' : '📄 Materi'}
              </button>
            ))}
          </div>
        </div>

        {/* Content area */}
        <div style={{ flex: 1, overflow: 'hidden', position: 'relative' }}>
          {activeTab === 'video' && mod.youtubeUrl && (
            <iframe
              src={mod.youtubeUrl}
              style={{ width: '100%', height: '100%', border: 'none' }}
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
              allowFullScreen
            />
          )}
          {activeTab === 'materi' && mod.gammaUrl && (
            <iframe
              src={mod.gammaUrl}
              style={{ width: '100%', height: '100%', border: 'none' }}
              allow="fullscreen"
            />
          )}
          {activeTab === 'video' && !mod.youtubeUrl && (
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', color: '#6b7280' }}>
              Video belum tersedia
            </div>
          )}
          {activeTab === 'materi' && !mod.gammaUrl && (
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', height: '100%', color: '#6b7280' }}>
              Materi belum tersedia
            </div>
          )}
        </div>

        {/* Footer navigasi */}
        <div style={{
          background: '#12122a',
          borderTop: '1px solid #1e1e3a',
          padding: '12px 20px',
          display: 'flex',
          alignItems: 'center',
          justifyContent: 'space-between',
          flexShrink: 0,
        }}>
          <button
            onClick={() => prevModule && router.push(`/modul/${prevModule.slug}`)}
            disabled={!prevModule}
            style={{
              background: prevModule ? '#1e1e3a' : 'transparent',
              border: '1px solid',
              borderColor: prevModule ? '#374151' : 'transparent',
              color: prevModule ? '#e2e8f0' : '#374151',
              padding: '8px 20px',
              borderRadius: '8px',
              cursor: prevModule ? 'pointer' : 'not-allowed',
              fontSize: '0.85rem',
            }}
          >
            ← {prevModule ? prevModule.title : 'Awal'}
          </button>

          <button
            onClick={() => router.push('/dashboard')}
            style={{
              background: 'transparent',
              border: 'none',
              color: '#7c3aed',
              cursor: 'pointer',
              fontSize: '0.85rem',
            }}
          >
            ⊞ Semua Modul
          </button>

          <button
            onClick={() => nextModule && router.push(`/modul/${nextModule.slug}`)}
            disabled={!nextModule}
            style={{
              background: nextModule ? '#7c3aed' : 'transparent',
              border: '1px solid',
              borderColor: nextModule ? '#7c3aed' : '#374151',
              color: nextModule ? 'white' : '#374151',
              padding: '8px 20px',
              borderRadius: '8px',
              cursor: nextModule ? 'pointer' : 'not-allowed',
              fontSize: '0.85rem',
            }}
          >
            {nextModule ? nextModule.title : 'Selesai'} →
          </button>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 4: Test manual**

```bash
npm run dev
```

Masuk ke `/dashboard` → klik modul → tampil halaman viewer full-screen. Test tab Video dan Materi. Test navigasi ← →. Test sidebar ☰.

- [ ] **Step 5: Commit**

```bash
git add components/ app/modul/
git commit -m "feat: module viewer full-screen dengan sidebar collapse"
```

---

## Phase 5: Admin Panel

### Task 16: Admin Layout + Guard

- [ ] **Step 1: Tulis admin layout**

Tulis `D:/VIBE CODING VERSION/aiguild/app/admin/layout.js`:
```js
import { getSession } from '@/lib/auth'
import { redirect } from 'next/navigation'
import Link from 'next/link'

export default async function AdminLayout({ children }) {
  const session = await getSession()
  if (!session?.isAdmin) redirect('/dashboard')

  const navItems = [
    { href: '/admin', label: 'Dashboard' },
    { href: '/admin/users', label: 'Users' },
    { href: '/admin/modules', label: 'Modul' },
    { href: '/admin/purchases', label: 'Pembelian' },
  ]

  return (
    <div style={{ display: 'flex', minHeight: '100vh', background: '#0a0a16' }}>
      <aside style={{
        width: '200px',
        background: '#12122a',
        borderRight: '1px solid #1e1e3a',
        padding: '1.5rem 0',
        flexShrink: 0,
      }}>
        <div style={{ padding: '0 1rem 1.5rem', color: '#f59e0b', fontWeight: '700', fontSize: '0.9rem' }}>
          AI Guild Admin
        </div>
        <nav>
          {navItems.map(item => (
            <Link
              key={item.href}
              href={item.href}
              style={{
                display: 'block',
                padding: '10px 1rem',
                color: '#9ca3af',
                textDecoration: 'none',
                fontSize: '0.9rem',
              }}
            >
              {item.label}
            </Link>
          ))}
        </nav>
      </aside>
      <main style={{ flex: 1, padding: '2rem' }}>
        {children}
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Buat admin user pertama via Prisma Studio**

```bash
npx prisma studio
```

Buka tabel `users` → edit row kamu → set `is_admin = true` → Save.

- [ ] **Step 3: Commit**

```bash
git add app/admin/layout.js
git commit -m "feat: admin layout dengan is_admin guard"
```

---

### Task 17: Admin Dashboard Stats

- [ ] **Step 1: Tulis app/admin/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/admin/page.js`:
```js
import { prisma } from '@/lib/db'

export default async function AdminDashboard() {
  const [totalUsers, totalPurchases, recentPurchases] = await Promise.all([
    prisma.user.count(),
    prisma.purchase.count(),
    prisma.purchase.findMany({
      take: 5,
      orderBy: { purchasedAt: 'desc' },
      include: { user: true },
    }),
  ])

  const stats = [
    { label: 'Total User', value: totalUsers, color: '#7c3aed' },
    { label: 'Total Pembelian', value: totalPurchases, color: '#10b981' },
  ]

  return (
    <div>
      <h1 style={{ color: '#e2e8f0', marginBottom: '2rem' }}>Dashboard</h1>

      <div style={{ display: 'flex', gap: '1rem', marginBottom: '2rem', flexWrap: 'wrap' }}>
        {stats.map(s => (
          <div key={s.label} style={{
            background: '#12122a',
            border: `1px solid ${s.color}33`,
            borderRadius: '12px',
            padding: '1.5rem',
            minWidth: '160px',
          }}>
            <div style={{ color: s.color, fontSize: '2rem', fontWeight: '700' }}>{s.value}</div>
            <div style={{ color: '#9ca3af', fontSize: '0.85rem', marginTop: '4px' }}>{s.label}</div>
          </div>
        ))}
      </div>

      <h2 style={{ color: '#e2e8f0', fontSize: '1rem', marginBottom: '1rem' }}>Pembelian Terbaru</h2>
      <div style={{ background: '#12122a', border: '1px solid #1e1e3a', borderRadius: '12px', overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr style={{ borderBottom: '1px solid #1e1e3a' }}>
              {['Email', 'Platform', 'Tanggal'].map(h => (
                <th key={h} style={{ padding: '12px 16px', textAlign: 'left', color: '#6b7280', fontSize: '0.8rem', fontWeight: '600' }}>{h}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {recentPurchases.map(p => (
              <tr key={p.id} style={{ borderBottom: '1px solid #1e1e3a' }}>
                <td style={{ padding: '12px 16px', color: '#e2e8f0', fontSize: '0.9rem' }}>{p.user.email}</td>
                <td style={{ padding: '12px 16px' }}>
                  <span style={{
                    background: p.source === 'lynkid' ? '#1e3a1e' : '#1a1e3a',
                    color: p.source === 'lynkid' ? '#4ade80' : '#60a5fa',
                    padding: '2px 8px',
                    borderRadius: '4px',
                    fontSize: '0.75rem',
                  }}>
                    {p.source}
                  </span>
                </td>
                <td style={{ padding: '12px 16px', color: '#9ca3af', fontSize: '0.85rem' }}>
                  {new Date(p.purchasedAt).toLocaleDateString('id-ID')}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add app/admin/page.js
git commit -m "feat: admin dashboard dengan stats dan recent purchases"
```

---

### Task 18: Admin Users Page

- [ ] **Step 1: Tulis API route revoke akses**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/admin/users/[id]/revoke/route.js`:
```js
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db'
import { getSession } from '@/lib/auth'

export async function POST(request, { params }) {
  const session = await getSession()
  if (!session?.isAdmin) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  const { id } = await params
  await prisma.purchase.deleteMany({ where: { userId: id } })
  return NextResponse.json({ ok: true })
}
```

- [ ] **Step 2: Tulis app/admin/users/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/admin/users/page.js`:
```js
'use client'

import { useEffect, useState } from 'react'

export default function AdminUsersPage() {
  const [users, setUsers] = useState([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    fetch('/api/admin/users')
      .then(r => r.json())
      .then(data => { setUsers(data); setLoading(false) })
  }, [])

  async function handleRevoke(userId) {
    if (!confirm('Cabut akses user ini?')) return
    await fetch(`/api/admin/users/${userId}/revoke`, { method: 'POST' })
    setUsers(prev => prev.filter(u => u.id !== userId))
  }

  if (loading) return <p style={{ color: '#9ca3af' }}>Memuat...</p>

  return (
    <div>
      <h1 style={{ color: '#e2e8f0', marginBottom: '1.5rem' }}>Users ({users.length})</h1>
      <div style={{ background: '#12122a', border: '1px solid #1e1e3a', borderRadius: '12px', overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr style={{ borderBottom: '1px solid #1e1e3a' }}>
              {['Email', 'Terdaftar', 'Pembelian', 'Aksi'].map(h => (
                <th key={h} style={{ padding: '12px 16px', textAlign: 'left', color: '#6b7280', fontSize: '0.8rem' }}>{h}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {users.map(u => (
              <tr key={u.id} style={{ borderBottom: '1px solid #1e1e3a' }}>
                <td style={{ padding: '12px 16px', color: '#e2e8f0' }}>{u.email}</td>
                <td style={{ padding: '12px 16px', color: '#9ca3af', fontSize: '0.85rem' }}>
                  {new Date(u.createdAt).toLocaleDateString('id-ID')}
                </td>
                <td style={{ padding: '12px 16px', color: '#9ca3af' }}>{u._count?.purchases ?? 0}</td>
                <td style={{ padding: '12px 16px' }}>
                  {!u.isAdmin && (
                    <button
                      onClick={() => handleRevoke(u.id)}
                      style={{
                        background: 'transparent',
                        border: '1px solid #dc2626',
                        color: '#f87171',
                        padding: '4px 12px',
                        borderRadius: '4px',
                        cursor: 'pointer',
                        fontSize: '0.8rem',
                      }}
                    >
                      Cabut Akses
                    </button>
                  )}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Tulis API route GET users**

Tulis `D:/VIBE CODING VERSION/aiguild/app/api/admin/users/route.js`:
```js
import { NextResponse } from 'next/server'
import { prisma } from '@/lib/db'
import { getSession } from '@/lib/auth'

export async function GET() {
  const session = await getSession()
  if (!session?.isAdmin) {
    return NextResponse.json({ error: 'Forbidden' }, { status: 403 })
  }

  const users = await prisma.user.findMany({
    orderBy: { createdAt: 'desc' },
    include: { _count: { select: { purchases: true } } },
  })

  return NextResponse.json(users)
}
```

- [ ] **Step 4: Commit**

```bash
git add app/admin/users/ app/api/admin/
git commit -m "feat: admin users page + revoke access"
```

---

### Task 19: Admin Purchases + Modules Pages

- [ ] **Step 1: Tulis app/admin/purchases/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/admin/purchases/page.js`:
```js
import { prisma } from '@/lib/db'

export default async function AdminPurchasesPage() {
  const purchases = await prisma.purchase.findMany({
    orderBy: { purchasedAt: 'desc' },
    include: { user: true },
  })

  return (
    <div>
      <h1 style={{ color: '#e2e8f0', marginBottom: '1.5rem' }}>Pembelian ({purchases.length})</h1>
      <div style={{ background: '#12122a', border: '1px solid #1e1e3a', borderRadius: '12px', overflow: 'hidden' }}>
        <table style={{ width: '100%', borderCollapse: 'collapse' }}>
          <thead>
            <tr style={{ borderBottom: '1px solid #1e1e3a' }}>
              {['Email', 'Platform', 'Order ID', 'Tanggal'].map(h => (
                <th key={h} style={{ padding: '12px 16px', textAlign: 'left', color: '#6b7280', fontSize: '0.8rem' }}>{h}</th>
              ))}
            </tr>
          </thead>
          <tbody>
            {purchases.map(p => (
              <tr key={p.id} style={{ borderBottom: '1px solid #1e1e3a' }}>
                <td style={{ padding: '12px 16px', color: '#e2e8f0' }}>{p.user.email}</td>
                <td style={{ padding: '12px 16px' }}>
                  <span style={{
                    background: p.source === 'lynkid' ? '#1e3a1e' : '#1a1e3a',
                    color: p.source === 'lynkid' ? '#4ade80' : '#60a5fa',
                    padding: '2px 8px', borderRadius: '4px', fontSize: '0.75rem',
                  }}>{p.source}</span>
                </td>
                <td style={{ padding: '12px 16px', color: '#9ca3af', fontSize: '0.85rem' }}>{p.orderId ?? '—'}</td>
                <td style={{ padding: '12px 16px', color: '#9ca3af', fontSize: '0.85rem' }}>
                  {new Date(p.purchasedAt).toLocaleDateString('id-ID')}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Tulis app/admin/modules/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/admin/modules/page.js`:
```js
import { prisma } from '@/lib/db'

export default async function AdminModulesPage() {
  const modules = await prisma.module.findMany({ orderBy: { orderIndex: 'asc' } })

  return (
    <div>
      <h1 style={{ color: '#e2e8f0', marginBottom: '0.5rem' }}>Modul ({modules.length})</h1>
      <p style={{ color: '#6b7280', fontSize: '0.85rem', marginBottom: '1.5rem' }}>
        Edit konten modul via <code style={{ color: '#a78bfa' }}>lib/modules-seed.js</code> lalu jalankan <code style={{ color: '#a78bfa' }}>npm run seed</code>
      </p>
      <div style={{ display: 'flex', flexDirection: 'column', gap: '10px' }}>
        {modules.map((m, i) => (
          <div key={m.id} style={{
            background: '#12122a',
            border: '1px solid #1e1e3a',
            borderRadius: '10px',
            padding: '1rem 1.25rem',
          }}>
            <div style={{ display: 'flex', alignItems: 'center', justifyContent: 'space-between' }}>
              <div>
                <span style={{ color: '#7c3aed', fontSize: '0.75rem' }}>#{i + 1}</span>
                <span style={{ color: '#e2e8f0', marginLeft: '10px', fontWeight: '600' }}>{m.title}</span>
              </div>
              <div style={{ display: 'flex', gap: '8px' }}>
                {m.youtubeUrl && <span style={{ color: '#4ade80', fontSize: '0.75rem' }}>▶ Video</span>}
                {m.gammaUrl && <span style={{ color: '#60a5fa', fontSize: '0.75rem' }}>📄 Materi</span>}
              </div>
            </div>
            <div style={{ color: '#6b7280', fontSize: '0.8rem', marginTop: '4px' }}>/{m.slug}</div>
          </div>
        ))}
      </div>
    </div>
  )
}
```

- [ ] **Step 3: Commit**

```bash
git add app/admin/purchases/ app/admin/modules/
git commit -m "feat: admin purchases + modules pages"
```

---

## Phase 6: Landing Page + Monitoring + Deploy

### Task 20: Landing Page

- [ ] **Step 1: Tulis app/page.js**

Tulis `D:/VIBE CODING VERSION/aiguild/app/page.js`:
```js
import Link from 'next/link'

export default function LandingPage() {
  return (
    <div style={{ minHeight: '100vh', background: '#0a0a16', color: '#e2e8f0' }}>
      <header style={{
        padding: '1.5rem 2rem',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'space-between',
        borderBottom: '1px solid #1e1e3a',
      }}>
        <span style={{ color: '#a78bfa', fontWeight: '700', fontSize: '1.2rem' }}>AI Guild</span>
        <Link href="/login" style={{
          background: '#7c3aed',
          color: 'white',
          padding: '8px 20px',
          borderRadius: '8px',
          textDecoration: 'none',
          fontSize: '0.9rem',
          fontWeight: '600',
        }}>
          Masuk
        </Link>
      </header>

      <main style={{ maxWidth: '720px', margin: '0 auto', padding: '5rem 2rem', textAlign: 'center' }}>
        <div style={{
          display: 'inline-block',
          background: '#1e1e3a',
          border: '1px solid #7c3aed33',
          borderRadius: '20px',
          padding: '6px 16px',
          color: '#a78bfa',
          fontSize: '0.8rem',
          marginBottom: '1.5rem',
          letterSpacing: '0.05em',
        }}>
          VIBE CODING COMMUNITY
        </div>

        <h1 style={{ fontSize: 'clamp(2rem, 5vw, 3rem)', fontWeight: '800', lineHeight: '1.2', marginBottom: '1rem' }}>
          Belajar <span style={{ color: '#7c3aed' }}>Vibe Coding</span>
          <br />dari Nol Sampai Deploy
        </h1>

        <p style={{ color: '#9ca3af', fontSize: '1.1rem', lineHeight: '1.7', marginBottom: '2.5rem', maxWidth: '500px', margin: '0 auto 2.5rem' }}>
          Kursus praktis untuk non-programmer yang mau bangun produk digital menggunakan AI. Tidak perlu hafal syntax.
        </p>

        <div style={{ display: 'flex', gap: '1rem', justifyContent: 'center', flexWrap: 'wrap' }}>
          <a
            href="https://lynk.id/aiguild"
            target="_blank"
            rel="noopener noreferrer"
            style={{
              background: '#7c3aed',
              color: 'white',
              padding: '14px 32px',
              borderRadius: '10px',
              textDecoration: 'none',
              fontWeight: '700',
              fontSize: '1rem',
            }}
          >
            Beli Kelas Sekarang
          </a>
          <Link href="/login" style={{
            background: 'transparent',
            color: '#a78bfa',
            padding: '14px 32px',
            borderRadius: '10px',
            textDecoration: 'none',
            fontWeight: '600',
            fontSize: '1rem',
            border: '1px solid #7c3aed44',
          }}>
            Sudah Beli? Masuk
          </Link>
        </div>
      </main>
    </div>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add app/page.js
git commit -m "feat: landing page dengan CTA beli kelas"
```

---

### Task 21: PostHog Integration

- [ ] **Step 1: Install PostHog**

```bash
npm install posthog-js
```

- [ ] **Step 2: Buat PostHog provider**

Tulis `D:/VIBE CODING VERSION/aiguild/components/PostHogProvider.js`:
```js
'use client'

import posthog from 'posthog-js'
import { PostHogProvider as PHProvider } from 'posthog-js/react'
import { useEffect } from 'react'

export default function PostHogProvider({ children }) {
  useEffect(() => {
    if (typeof window !== 'undefined' && process.env.NEXT_PUBLIC_POSTHOG_KEY) {
      posthog.init(process.env.NEXT_PUBLIC_POSTHOG_KEY, {
        api_host: process.env.NEXT_PUBLIC_POSTHOG_HOST ?? 'https://app.posthog.com',
        capture_pageview: true,
        capture_pageleave: true,
      })
    }
  }, [])

  return <PHProvider client={posthog}>{children}</PHProvider>
}
```

- [ ] **Step 3: Wrap di root layout**

Edit `D:/VIBE CODING VERSION/aiguild/app/layout.js`, tambahkan PostHogProvider:
```js
import PostHogProvider from '@/components/PostHogProvider'

export const metadata = { title: 'AI Guild', description: 'Platform pembelajaran vibe coding' }

export default function RootLayout({ children }) {
  return (
    <html lang="id">
      <body style={{ margin: 0, fontFamily: 'system-ui, sans-serif' }}>
        <PostHogProvider>
          {children}
        </PostHogProvider>
      </body>
    </html>
  )
}
```

- [ ] **Step 4: Daftar PostHog dan isi env**

Buka `https://app.posthog.com` → daftar → buat project → copy API key ke `.env.local` sebagai `NEXT_PUBLIC_POSTHOG_KEY`.

- [ ] **Step 5: Commit**

```bash
git add components/PostHogProvider.js app/layout.js
git commit -m "feat: PostHog analytics integration"
```

---

### Task 22: Sentry Integration

- [ ] **Step 1: Setup Sentry wizard**

```bash
npx @sentry/wizard@latest -i nextjs
```

Ikuti prompt: login Sentry, pilih project, wizard akan otomatis buat `sentry.client.config.js`, `sentry.server.config.js`, dan update `next.config.mjs`.

- [ ] **Step 2: Daftar Sentry**

Buka `https://sentry.io` → daftar → buat project Next.js → copy DSN ke `.env.local` sebagai `SENTRY_DSN`.

- [ ] **Step 3: Commit**

```bash
git add sentry.client.config.js sentry.server.config.js next.config.mjs
git commit -m "feat: Sentry error tracking integration"
```

---

### Task 23: Dockerfile + Deploy ke Coolify

- [ ] **Step 1: Tulis Dockerfile**

Tulis `D:/VIBE CODING VERSION/aiguild/Dockerfile`:
```dockerfile
FROM node:20-alpine AS deps
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production

FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npx prisma generate
RUN npm run build

FROM node:20-alpine AS runner
WORKDIR /app
ENV NODE_ENV=production

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static
COPY --from=builder /app/node_modules/.prisma ./node_modules/.prisma

USER nextjs
EXPOSE 3000
ENV PORT=3000

CMD ["node", "server.js"]
```

- [ ] **Step 2: Aktifkan standalone output di next.config.mjs**

Edit `D:/VIBE CODING VERSION/aiguild/next.config.mjs`:
```js
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
}

export default nextConfig
```

- [ ] **Step 3: Push ke GitHub**

```bash
git add Dockerfile next.config.mjs
git commit -m "feat: Dockerfile + standalone output untuk deploy"
git remote add origin https://github.com/<username>/aiguild.git
git push -u origin main
```

- [ ] **Step 4: Setup di Coolify**

Di Coolify dashboard:
1. Klik **+ New Resource** → **Application** → **GitHub**
2. Pilih repo `aiguild`
3. Build pack: **Dockerfile**
4. Set environment variables (semua dari `.env.example` dengan nilai production)
5. Set DATABASE_URL ke connection string PostgreSQL yang sudah dibuat di Task 1
6. Klik **Deploy**

- [ ] **Step 5: Jalankan migrasi di production**

Di Coolify, buka terminal container Next.js:
```bash
npx prisma migrate deploy
node scripts/seed.js
```

- [ ] **Step 6: Set domain + SSL**

Di Coolify:
1. Masuk ke settings aplikasi → **Domains**
2. Tambah domain kamu (misal: `aiguild.id`)
3. Coolify + Traefik otomatis aktifkan SSL via Let's Encrypt

- [ ] **Step 7: Daftarkan webhook URL ke Lynk.id dan Mayar.id**

- Lynk.id: masuk dashboard → Settings → Webhook → isi `https://aiguild.id/api/webhook/lynkid`
- Mayar.id: masuk dashboard → Integrasi → Webhook → isi `https://aiguild.id/api/webhook/mayar`
- Copy webhook secret masing-masing ke environment variables Coolify

- [ ] **Step 8: Smoke test production**

1. Buka `https://aiguild.id` — landing page muncul
2. Buka `/login` — form muncul
3. Test webhook Lynk.id dengan curl:
   ```bash
   curl -X POST https://aiguild.id/api/webhook/lynkid \
     -H "Content-Type: application/json" \
     -H "x-lynkid-signature: <test-sig>" \
     -d '{"event":"order.paid","data":{"buyer_email":"test@example.com","order_id":"TEST-001"}}'
   ```
4. Cek admin panel `/admin` — user test muncul
5. Klik magic link dari email — masuk ke `/dashboard`

- [ ] **Step 9: Commit final**

```bash
git add .
git commit -m "chore: ready for production"
```

---

## Checklist Sebelum Launch

- [ ] Semua smoke test di Task 23 Step 8 lulus
- [ ] PostHog menerima events (cek di dashboard)
- [ ] Sentry menerima test error (jalankan `npx sentry-wizard` → test)
- [ ] Webhook Lynk.id dan Mayar.id terdaftar dengan URL production
- [ ] Environment variables production lengkap di Coolify
- [ ] SSL aktif (HTTPS berjalan)
- [ ] User admin terdaftar di database production
- [ ] `/security-review` dijalankan, tidak ada temuan critical/high
